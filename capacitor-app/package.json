{
  "name": "price-watch-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android shell for Price Watch (loads deployed Next.js web app)",
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "run:android": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/app": "^7.0.2",
    "@capacitor/browser": "^7.0.2",
    "@capacitor/core": "^7.4.2",
    "@capacitor/push-notifications": "^7.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2",
    "typescript": "^5.8.3"
  },
  "capacitor": {
    "platforms": [
      "android"
    ]
  }
}
